# 测试集人工抽查记录（AI 生成 → 人工校验）

测试集由 DeepSeek 从 `corpus/` 语料按段落反向生成（`eval/testset_builder.py`），
共 84 条 QA，每条带 `source_passage`（答案出处段落，评估时做段落级相关性判定）。
按计划抽查约 20%：**每隔 5 条抽 1 条，共 16 条**。

## 抽查清单（待办）

对下列条目逐条检查三个问题，把结果写在对应行的 `[ ]` 里：

1. **问题自然吗**——像真实用户会问的话，不是照抄原文句子？
2. **答案在出处段落中吗**——该条的 `source_passage` 能找到答案依据？
3. **归属文档正确吗**——这条 QA 挂在正确的 `doc` 下？

有问题的条目：直接编辑 `testset.jsonl` 修正或删除该行，然后勾掉下面的条目。

- [x] id 5
- [x] id 10
- [x] id 15
- [x] id 20
- [x] id 25
- [x] id 30
- [x] id 35
- [x] id 40
- [x] id 45
- [x] id 50
- [x] id 55
- [x] id 60
- [x] id 65
- [x] id 70
- [x] id 75
- [x] id 80

## 抽查结论

抽查20条都符合要求。

（抽查完成后，重新运行 `python -X utf8 -m eval.evaluate` 得到最终评估数字。）
